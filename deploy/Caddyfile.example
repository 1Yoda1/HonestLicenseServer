# Existing dedicated API hostname.
api.honestflow.ru {
    encode zstd gzip
    reverse_proxy 127.0.0.1:5498
}

# Same-origin website layout used on the Ubuntu server.
honestflow.ru {
    encode zstd gzip

    handle /api/* {
        reverse_proxy 127.0.0.1:5498
    }

    handle {
        root * /opt/honestserver/site
        try_files {path} /index.html
        file_server
    }
}
